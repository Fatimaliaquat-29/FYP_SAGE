# S.A.G.E. FYP — Full Technical History & Context
**Branch:** TCN_sanawar
**Phase:** Real-time hybrid detection (heuristic + LSTM/TCN/RF), a first-pass gait/fall-risk module, and an August 2026 repo-wide cleanup/audit pass
**Last Updated:** August 2026 (added Section 16 — project cleanup, validation audit, and a documentation gap fix; see Section 16 for a changelog against the previous version of this file)

---

## 1. System Architecture Overview

The S.A.G.E. (Smart Automated Guardian & Evaluator) pipeline detects human falls from video footage. It has two parallel detection layers:

1. **Pose Estimation:** MediaPipe Pose Landmarker (`pose_landmarker_full.task`) extracts 33 body landmarks (x, y coordinates, normalized 0–1) from each video frame.
2. **Heuristic Layer (Rule-based, `src/posture/pipeline_utils.py`):** Analyzes geometry and velocity frame-by-frame using scale-invariant joint angles (torso, knee, hip) as the primary posture signal, with body-height-ratio as an occlusion fallback only.
3. **LSTM Layer (Temporal, `models/lstm_posture.keras`):** Analyzes 30-frame rolling windows of **normalized, motion-aware** keypoints (see Section 3) for complex temporal patterns.
4. **Hybrid Logic:** `fall_detected = Heuristic OR LSTM`. Implemented in `hybrid_evaluate.py` (batch evaluation harness) and natively in `pipeline_utils.classify_posture_and_fall(lstm_classifier=...)` (production path).
5. **Real-time entry point:** `realtime_fall_detection.py` — live camera or video file → MediaPipe → hybrid → a **debounced** alert (requires the fall signal in ≥4 of the last 12 frames before alerting, then latches for 5s). This is the actual deployable/demoable script.

---

## 2. The Heuristic Layer (`pipeline_utils.py`)

### How It Works
- Extracts geometric features from MediaPipe landmarks every frame:
  - **Torso Angle:** angle of the spine relative to vertical (0° = upright, 90° = horizontal)
  - **Knee Angle / Hip Angle:** scale-invariant joint angles, the *primary* posture signal (not body-height ratio — that was the original distance-dependent bug, fixed long before this document's current revision)
  - **Torso Angular Velocity:** rate of change of torso angle, time-normalized to deg/sec
  - **Hip Velocity:** hip-center speed, normalized by elapsed time AND by body scale (body-heights/sec), so the same real-world speed reads the same regardless of camera distance or frame rate
- **Posture classification** (angle-based, `ANGLE_STANDING_MIN=143°`, `ANGLE_SITTING_MAX=125°`, with a body-height-ratio fallback only when knees/hips are occluded).
- **Temporal smoothing:** a posture must see 2 *consecutive* raw "Lying" frames before it latches (added to stop a single-frame torso-angle spike during a fast sit-down from sticking as "Lying" — see Section 5).
- **Fall Detection Triggers (3 modes, current constants — re-derived from real data in Section 5, not hand-picked):**
  - **`rapid_fall`** (posture already confirmed Lying): sustained hip velocity + sustained torso angular velocity + **real downward hip displacement** (`FALL_HIP_DESCENT_MIN=0.06` frame-heights) — the hip-descent requirement specifically distinguishes a genuine collapse-to-floor from someone reclining in a chair (torso pitches back, hips stay at seat height).
  - **`pre_lying_fall`** (posture not yet Lying — earlier warning, higher bar since there's no Lying confirmation to lean on): requires the torso to actually be pitching toward horizontal (`torso_angle >= 45°`) AND a higher, dedicated angular-velocity floor (`FALL_PRELYING_ANGVEL_FLOOR=250°/sec` sustained `FALL_PRELYING_SUSTAIN=4` frames). This gate is what stops a fast sit-down (which spikes angular velocity while staying upright in the seat) from tripping a false alarm before Lying is even confirmed.
  - **`sustained_lying`** (slow-fall fallback): if the person has been continuously Lying for `LYING_PERSISTENCE_FRAMES=40` frames and was upright at some point this session, flag a fall even with no velocity spike at all. This is what catches a genuine slow elderly crumple that never trips the velocity-based triggers.

### Current Fall-Detection Constants (`pipeline_utils.py`, re-derived July 2026 — see Section 5)
```
FALL_VELOCITY_THRESHOLD      = 1.5    # body-heights/sec, "fast frame" floor
FALL_SUSTAINED_COUNT         = 2      # min fast frames in the lookback window
FALL_AVG_VELOCITY_FLOOR      = 1.0    # body-heights/sec
FALL_PEAK_VELOCITY_FLOOR     = 3.0    # body-heights/sec
FALL_ANGULAR_VELOCITY_FLOOR  = 150.0  # deg/sec
ANGVEL_SUSTAIN_FRAMES        = 3      # consecutive frames above the angvel floor
FALL_PRELYING_ANGVEL_FLOOR   = 250.0  # deg/sec (pre-lying trigger only, higher bar)
FALL_PRELYING_SUSTAIN        = 4
FALL_HIP_DESCENT_MIN         = 0.06   # fraction of frame height
LYING_PERSISTENCE_FRAMES     = 40     # frames of continuous Lying -> sustained_lying fallback
```

### Heuristic Strengths
- **Zero false positives** across all of `Testing/Sanawar Testing 7-22-26/` (the original 16-clip set) both before and after every fix in this document — every change so far has been regression-tested against it.
- Latency substantially improved after the July 2026 threshold re-derivation (e.g. `normal` clip: 81→43 frames) without introducing any new false positives, once three additional FP mechanisms the retune exposed were fixed (see Section 5).

### Heuristic Weaknesses (known, unresolved)
- **Falls whose direction runs along the camera's optical axis:** if a person falls toward/away from the camera rather than across its field of view, the body foreshortens in 2D instead of visibly rotating — `torso_angle` never registers "horizontal." Found via `Backward_fall.mp4` in the July 2026 unseen-footage validation (Section 8) — confirmed a **fundamental 2D single-camera limitation**, not a threshold problem (the LSTM misses it too, same reason: same underlying landmarks). Needs a product decision (camera placement guideline, or a secondary cue independent of trunk rotation), not more tuning.

---

## 3. The LSTM Layer

### Goal
Learn the temporal dynamics of falling across a 30-frame window to detect slow falls faster than the heuristic, and to independently corroborate the heuristic via the OR-gate.

### Architecture (unchanged)
```
Input (30 frames × 132 features)
  → LSTM(64 units) → Dropout(0.3)
  → LSTM(32 units) → Dropout(0.3)
  → Dense(5, softmax)
```
**Output classes:** Fall, Lying, Sitting, Standing, Unknown

### Input Features — CHANGED, this was the single biggest fix in this document's history
The model **used to** train directly on raw MediaPipe (x, y) screen coordinates, normalized only to the [0,1] image frame. This has **no scale or translation invariance**: a person standing far from the camera occupies the same small vertical extent in the frame as someone lying down, and the model had no structural way to tell those apart. Diagnostic proof: a clean, motionless Standing clip (torso ~2°, knee/hip angles ~170°, velocity ~0.2 body-heights/sec — unambiguous by every scale-invariant measure the heuristic computes) still scored ~66% "Fall" probability from the old model.

**Fix:** new shared module `src/posture/lstm/lstm_features.py`, the single source of truth for the LSTM's input representation, used identically by training (`lstm_dataset.py`), production inference (`lstm_classifier.py`), and batch evaluation (`hybrid_evaluate.py`):
- Every frame's 33 landmarks are re-expressed as **hip-centered, torso-length-scaled position** (66 dims) — invariant to camera distance/framing, but keeps full body shape (unlike reducing to just joint angles).
- Plus **frame-to-frame velocity of that normalized position** (66 dims) — explicit motion signal.
- Total: 132 input features (was 66).
- A `col_medians` array (computed from real training data, saved into `models/lstm_label_encoder.json`) is used for NaN-imputation at inference time — previously training and inference used *different, inconsistent* imputation (a real train/inference skew bug, also fixed).
- Synthetic training templates (`_make_standing_kps`/`_make_sitting_kps`/`_make_lying_kps`/`_make_fall_sequence` in `lstm_dataset.py`) are now expressed directly in this normalized space.
- **Inference contract:** `LSTMPostureClassifier.predict()` now needs `window_size + 1` raw frames (`raw_history_needed` property), not `window_size` — the extra leading frame supplies a real (not zero-padded) velocity for the oldest frame in the window.

### Training Data Sources
1. **UR Fall Dataset** (real RGB videos): ~30 fall + ADL sequences.
2. **UP-Fall Dataset** (3D skeleton data): 5 subjects × multiple fall activities.
3. **LeFD (Le2i Fall Detection dataset)** — added July 2026, see Section 4: 130 real annotated videos (96 falls + 34 ADL) across Coffee_room/Home scenes.

Total real frames: **62,202** (up from 20,136 before LeFD), across 282 sequences.

### A second root cause, found after the feature fix: no gate on "just fell" vs. "already lying"
After the feature-normalization fix, two clips regressed: `Lying_straight.mov` and `Lying_legs_straight.mov` (subject lying down for the *entire* clip, never seen Standing/Sitting) went from 0 false Fall frames to ~57-58% flagged. A single 30-frame window has no way to distinguish "just fell" from "was already lying when tracking started" — both look geometrically identical in isolation. The old raw-coordinate model avoided this by accident (a camera-specific shortcut), not genuine understanding.

**Fix:** gate the LSTM's fall signal on "has a genuine Standing/Sitting posture been observed at any point this session" — mirrors the heuristic's own pre-existing `_has_been_upright_this_session` safeguard. Implemented in both `pipeline_utils.py`'s production LSTM branch and `hybrid_evaluate.py`'s independent eval-harness LSTM path. Fully eliminated both false positives with zero effect on any true-positive latency.

### Original diagnostic tasks that led here (historical, kept for record)
Before the scale-invariance root cause above was found, an earlier diagnostic pass (window-composition analysis, sitting-FP mechanism trace, geometric coordinate comparison) discovered that 95% of "Fall"-class training windows were static post-fall lying frames rather than actual falling motion, and that geometrically identical static poses had contradictory labels ("Fall" if sourced from the end of a fall video, "Lying" if sourced from a resting video). This led to the **window-labeling fix** still in effect today in `lstm_dataset.py: build_windows_from_real_data()`: a window is labeled `"Fall"` if and only if at least one frame inside it is labeled `"Fall"`; otherwise it takes its last frame's label. `verify_labels.py` checks this invariant holds (should always read 100% of Fall windows containing a real transition frame, 0% pure-static contamination). That fix was necessary but not sufficient — it stopped the training *labels* from being contradictory, but the model was still learning from unnormalized coordinates, which is the deeper root cause resolved above.

---

## 4. LeFD Dataset Integration (July 2026)

Added `datasets/LeFD/` (Le2i Fall Detection dataset: Coffee_room_01/02, Home_01/02, Lecture_room, Office — 320×240 25FPS). Only Coffee_room_01/02 and Home_01/02 have per-frame fall annotations; Lecture_room and Office ship videos only with no ground truth, so they were excluded from ingestion.

`process_lefd_video`/`process_lefd_dataset` in `src/data_processing/build_lstm_datasets.py` reads each annotated video via cv2, runs MediaPipe per frame, and applies a state machine from the annotation's (fall_start, fall_end) frame numbers: before start = trust the heuristic's own posture label, [start,end] = force "Fall", after end = force "Lying". For the ~34 pure-ADL clips, `fall_detected` is forcibly clamped to `False` for the whole clip — necessary because the heuristic's own flag would otherwise leak "Fall" labels onto genuine non-fall Lying/Sitting frames.

**Result:** frames 20,136 → 62,202, Sitting class 1,235 → 5,395 frames (fixes the original class-imbalance problem). This dataset expansion was necessary but **not sufficient** — it fixed the Sitting-class confusion but the sustained false positives on Standing_1/Sit_3/Fast_Sit persisted until the feature-normalization fix (Section 3) was found afterward.

---

## 5. Fall-Threshold Re-Derivation From Data (July 2026)

**The root of the month-long "add a new clip, something breaks, patch a threshold, repeat" cycle:** the fall-detection thresholds were hand-fit to just 2-3 specific test clips (old code comments literally referenced "NF1 peak=26.36" and "Sit_3's fast sit-down (peak=11.09)" as the calibration points). A threshold with zero statistical margin, fit to a handful of examples, cannot generalize to a new clip by construction.

**Method:** replayed `classify_posture_and_fall` over all 130 real LeFD videos and compared peak hip-velocity + torso-angular-velocity between 96 annotated falls and 34 ADL clips.

**Key findings:**
1. The old `FALL_PEAK_VELOCITY_FLOOR=15.0` caught only **~17% of real falls** (real-fall peak velocity median is 5.1 body-heights/sec) — this single bug was forcing the majority of real falls to fall back on the slow 40-frame sustained-lying path, and was the direct cause of the 76-/81-/114-frame latencies seen before this fix.
2. **Hip velocity is a weak discriminator** on its own (falls median 5.1 vs. ADL median 4.1 body-heights/sec — heavy overlap).
3. **Torso angular velocity is the strong discriminator**, and it's cross-validated against the literature: our data's 5th-percentile of real-fall peak angular velocity is 177°/sec; the classic wearable-gyroscope fall-detection threshold in the literature is 3.1 rad/sec ≈ 178°/sec. Near-exact agreement.
4. **The real fall-vs-ADL discriminator is "does the motion end in Lying,"** not motion magnitude — a fast sit-down has comparable velocity/angular-velocity spikes to a real fall but doesn't end with the person on the floor. This is why the thresholds could be made recall-biased (aggressive) for the Lying-confirmed `rapid_fall` trigger while keeping a higher bar on the earlier `pre_lying_fall` trigger, which fires before Lying is confirmed.

New constants are listed in Section 2. Recall-biased by design: **for an elderly-safety system, a missed fall is far worse than a false alarm.**

### Three new false-positive mechanisms the retune exposed, and their fixes
Making the fall-motion floors more sensitive (as above) surfaced three new failure modes, each fixed with a physically-grounded gate rather than more threshold-nudging:
1. **Sticky-smoothing amplification:** a single-frame `torso_angle` spike during a fast sit-down briefly latched "Lying," and the "keep previous label when votes aren't unanimous" smoothing rule stretched that one frame into ~5 frames of Lying — long enough to trip the now-sensitive `rapid_fall` trigger. **Fix:** require two *consecutive* raw-Lying frames before actually entering Lying (costs a real fall +1 frame of latency).
2. **Pre-lying trigger fired on fast sit-downs:** these spike angular velocity while the trunk stays upright in the seat. **Fix:** gate `pre_lying_fall` on `torso_angle >= 45°` (the trunk must actually be pitching toward horizontal) plus its own higher, dedicated angular-velocity floor.
3. **Reclining in a chair read as a fall:** torso pitches back (54–67°, reading as "Lying"), but the hips stay at seat height — this doesn't drop the hips to the floor the way a real fall does. **Fix:** `rapid_fall` also requires real downward hip displacement over the lookback window (`FALL_HIP_DESCENT_MIN`). Deliberately **not** applied to the LSTM path — a very slow elderly crumple can descend less than this per 10-frame window, and gating the LSTM the same way risks missing exactly the slow-fall case the system exists to catch.

### Two other concrete (dormant) heuristic bugs fixed in the same pass
- `_lying_run_length == LYING_PERSISTENCE_FRAMES` (exact equality) → changed to `>=`. The old check could silently and permanently disable the sustained-lying fallback for an entire session if the counter ever skipped past exactly 40.
- The fall-detection lookback window used to silently treat any frame that lost landmark tracking as `velocity=0.0`, diluting the average/peak/fast-frame-count exactly during the motion blur a fast real fall tends to cause. Fixed to exclude those frames instead of zero-filling them.
- Both fixes were confirmed zero-regression (heuristic-only evaluation was byte-identical before/after on the full 16-clip set) — these were dormant landmines, not active bugs on the existing test set, but real risks for future footage.

---

## 6. Real-Time Detection

`realtime_fall_detection.py` (repo root) — live camera or video file → MediaPipe → the hybrid pipeline (heuristic OR LSTM, via `pipeline_utils.classify_posture_and_fall(lstm_classifier=...)`) → a **debounced** alert.

- **Why debounce:** a single-frame fall trigger can come from a landmark glitch. Alerting on every frame-level trigger causes false alarms, and false alarms cause alarm fatigue — which is just as dangerous as missing a fall. An alert only fires once the fall signal is present in `ALERT_MIN_HITS=4` of the last `ALERT_WINDOW=12` frames, then latches for `ALERT_HOLD_SECONDS=5.0` before it can re-arm.
- Uses wall-clock timestamps (correct `dt` for a live, variable-framerate feed).
- Caps retained frame history at 320 for bounded memory in a long-running session.
- Has an `on_alert(event: dict)` callback hook — wire in SMS/dashboard/email notification here.
- CLI flags: `--input` (camera index or video file path, default `0`), `--no-lstm` (heuristic only), `--no-display` (headless), `--alert-window`, `--alert-min-hits`, `--alert-hold`.
- See `INSTRUCTIONS.md` for exact run commands.

---

## 7. Ground-Truth Parser Now Supports 3 Dialects

Different recording batches produced different GT CSV shapes. `evaluate_real_footage.py: load_ground_truth()` (shared by `hybrid_evaluate.py`) auto-detects all three by checking whether the time string contains `:`:
1. `M:SS` + explicit label + `TRUE/FALSE` ignore flag.
2. `M:SS` + label + ignore flag + optional trailing `expected_fall_start, expected_fall_end` columns (bypasses window-derivation entirely).
3. Decimal-seconds + header row (`start_time,end_time,state,label`) — the format the team's recording tool exports. `state` values (`Standing`/`Sitting`/`Fall`/`Transition State`) are mapped to the Standing/Sitting/Lying/Unknown vocabulary the scorer expects (`Fall`→`Lying`, anything containing "transition" → ignored).

---

## 8. Validation Rounds (chronological)

### 8a. `Testing/Sanawar Testing 7-22-26/` (originally `test_footage/`) — the original 16-clip regression set
Used continuously throughout every fix above as the regression gate. Current state (post all fixes in this document): heuristic = 0 FP, 0 FN across all 16 clips. Hybrid = 6/6 real falls caught, 0 FN, small residual LSTM-only false positives on `Sit_1.mov` (17/157 frames) and `Sit_2.mov` (1/150 frames) — down from a pre-fix state where 8 of 10 true-no-fall clips had LSTM false positives, some sustained at 70–85% of the whole clip.

### 8b. `Testing/Hussain Testing 7-23-26/` (originally `hussain testing/`)
Contains `normal.mp4`/`old.mp4` — confirmed **byte-identical** (same file size, frame count, fps) to the `normal`/`old` clips in 8a. The only difference is the ground-truth annotation marks the fall window ~1 second tighter. Our detections are the exact same frames already validated as true positives against 8a's window; they simply land 4-55 frames after this folder's tighter cutoff. **A ground-truth annotation discrepancy between the two folders, not a pipeline regression.**

### 8c. `Testing/Sanawar Testing 7-25-26/` (originally `New Test Footage/`) — genuinely unseen team-recorded footage
The most trustworthy validation so far: 9 clips the pipeline had never been exposed to, covering the fall-variety list in `docs/sanity_check_clips.md` (forward/backward/side/slow/chair/far/off-axis/occluded falls). **Result: 8/9 caught, 0 false positives.**
- **`Backward_fall.mp4` — genuine miss (FN), both heuristic and LSTM.** Root cause: a fundamental single-camera 2D-projection limitation (see Section 2's "Heuristic Weaknesses"). Unresolved, needs a product decision.
- **`Chair_fall.mp4` — LSTM alone missed it (FN), heuristic caught it (TP), hybrid OR-gate worked as designed.** Likely an LSTM generalization gap (most fall training data is standing-height falls, not falling out of a seated position).
- **`Slow_fall.mp4` — led to discovering the scoring-window bug** (Section 9): the LSTM fired confidently from frame 68–123, almost exactly the GT's "Transition" (slow-collapse) segment (66–126) — detecting the fall essentially the instant it began. The scorer originally counted this as a false positive.
- Note: `Foward_fall.mp4` has a typo vs. `Forward_fall_gt.csv` in this folder — doesn't auto-pair via the batch-mode `*_gt.csv` stem-matching, needs a separate single-clip CLI invocation. Not renamed (left as the team recorded it).

### 8d. `testVideo.mp4` (deleted from the repo since) — ad-hoc spot-check, no ground truth
Reconstructed narrative: Standing→Sitting→Standing→Sitting→Lying (14.8s–22.5s)→Sitting→Standing. LSTM fired first (14.55s, just before posture even read Lying); heuristic's sustained-lying fallback confirmed almost exactly 40 frames after Lying began, matching `LYING_PERSISTENCE_FRAMES` precisely. A live debounced alert would have fired ~0.1s into the fall. **Caveat: this was the system's read of itself, never verified against an actual ground truth** — treat as circumstantial evidence of sane behavior, not confirmed accuracy.

---

## 9. Scoring-Window Fix

Found via 8c's `Slow_fall.mp4`: `get_fall_window()`'s fallback derivation (used whenever a GT file has no explicit `expected_fall_start`/`expected_fall_end` columns) built the scored window from *only* the "Lying" segment's start/end — completely ignoring the "Transition" segment where the actual falling motion happens. A detector that correctly fires *while the person is falling* (the ideal early-warning behavior, especially for a slow elderly crumple) was being scored as a false positive purely for detecting before the person had finished settling onto the floor.

**Fix:** the window's start now extends backward through any immediately-preceding GT segment marked `ignore=True` (a "Transition"/"Transition State" row). Only affects the fallback-derivation dialects — files with explicit `expected_fall_start`/`expected_fall_end` columns are untouched.

**Validated effect** (re-ran all of 8a and 8c, heuristic + hybrid): **zero clips changed TP/FN/FP status** — this is a pure measurement fix, not a behavior change. `Slow_fall`'s LSTM row flipped FP → TP +2f as expected. Latency numbers moved in both directions, and both directions are informative: some got bigger (revealing the old numbers were flattering us, measuring from an already-late reference point), others got smaller (revealing the heuristic/LSTM was *already* firing earlier than previously credited, during the transition phase, and the old narrow window was throwing that credit away). Net effect: latency numbers now measure "time from actual fall onset to alert" (the number that matters for a real caregiver) instead of "time from settled-on-floor to alert."

---

## 10. Current Status & What Still Needs Doing

### Done
- [x] Data leakage fix (random split → StratifiedGroupKFold) — pre-dates this document's current revision, still in effect.
- [x] LeFD dataset integration (Section 4).
- [x] LSTM feature-normalization fix — the real root cause of the LSTM false-positive crisis (Section 3).
- [x] Upright-gate fix for the "just fell vs. already lying" ambiguity (Section 3).
- [x] Fall-threshold re-derivation from real data + literature cross-check (Section 5).
- [x] Three FP-mechanism fixes exposed by the retune (Section 5).
- [x] Two dormant heuristic bugs fixed (Section 5).
- [x] Real-time detection entry point (Section 6).
- [x] Scoring-window fix (Section 9).
- [x] Validation across 4 rounds of footage, including genuinely unseen team-recorded clips (Section 8): 8/9 real falls caught with 0 false positives.
- [x] Recording guide for future validation clips: `docs/sanity_check_clips.md`.
- [x] Repo cleanup: removed one-off diagnostic scripts and superseded result snapshots whose findings are already captured in this document; `data/`, `datasets/`, and `Testing/` excluded from git via `.gitignore` (see `INSTRUCTIONS.md` for how to regenerate `data/` locally).
- [x] **TCN alternative architecture implemented, trained, and compared against the LSTM on real footage** (Section 13) — TCN wins on every measured axis (accuracy, fall recall, parameter count, latency) in the current run, but see Section 13's reproducibility caveat before treating that as settled.

### Pending / Open
- [ ] **`Backward_fall.mp4`'s camera-axis miss** (Section 2/8c) — needs a product decision: camera-placement guideline, or a secondary detection cue independent of trunk rotation (e.g. rapid apparent-size/bounding-box shrinkage). Confirmed in Section 13 that the TCN misses this clip too, for the same underlying single-camera-projection reason.
- [ ] **No real elderly fall data.** Every fall in training (UR, UP-Fall, LeFD) is a fit adult *simulating* a fall — fast, athletic, often braced. Nothing resembles an actual frail/slow/unbraced elderly collapse, or someone using a cane/walker. This is judged the single highest-value gap remaining — higher priority than further tuning. Applies equally to the TCN, which was trained on the same UR data.
- [ ] **Precision has only been validated on old footage, not new.** `Testing/Sanawar Testing 7-25-26/` was all fall clips — recall-only validation on new footage. The negative/ADL half of `docs/sanity_check_clips.md` (fast sit, recline, bend, walk) hasn't been recorded as genuinely new clips yet, so it's unconfirmed whether the false-positive fixes generalize the same way the recall did.
- [ ] `Chair_fall.mp4`'s LSTM-alone miss — not urgent (heuristic covers it in the OR-gate) but worth more fall-from-seated training data if pursued.
- [ ] Sit_1.mov / Sit_2.mov residual small LSTM false positives (17/157 and 1/150 frames) — low severity, not investigated further.
- [ ] Chair_fall's posture-tracking *accuracy* (not fall detection) is shaky after landing in an unusual pose — doesn't affect alerting, not chased further.
- [ ] **TF op-level non-determinism** (Section 13) — `keras.utils.set_random_seed(42)` was added to both trainers but real-footage results still vary run-to-run; `tf.config.experimental.enable_op_determinism()` plus a multi-seed sweep is needed before any hyperparameter conclusion (LSTM vs TCN or within either architecture) can be trusted.
- [ ] Only 8 labelled real clips exist in `test_footage/` — one flipped clip swings fall-detection recall by 12.5 points, which is larger than most of the effects anyone will want to measure. More real clips are worth more than more tuning right now (same conclusion Section 10 already reached for the LSTM/heuristic work, now doubly true with a second model to compare).
- [ ] Nothing has been pushed to the remote yet as of this writing — working tree is prepared (`.gitignore` updated, `data/` untracked) but the user has not yet confirmed the final push.

---

## 11. Key Files Reference

| File | Purpose |
|---|---|
| `src/posture/pipeline_utils.py` | Heuristic detection logic — posture classification + all 3 fall triggers |
| `src/posture/lstm/lstm_features.py` | **New.** Single source of truth for the LSTM's input feature representation (normalized position + velocity) |
| `src/posture/lstm/lstm_dataset.py` | Builds sliding-window training data (real + synthetic) |
| `src/posture/lstm/lstm_trainer.py` | Trains the LSTM, saves `col_medians` into the label encoder |
| `src/posture/lstm/lstm_classifier.py` | Production inference wrapper (`LSTMPostureClassifier`) |
| `src/data_processing/build_lstm_datasets.py` | Raw video/image datasets (UR, UP-Fall, LeFD) → keypoint CSVs |
| `src/data_processing/build_ur_dataset_from_data_root.py` | **New (Section 13).** Adapter that reuses `build_lstm_datasets.py`'s UR-processing function unchanged, pointed at `data/ADL`/`data/Fall` (this project's actual data drop location) instead of `datasets/UR_data/ADL`/`Fall` |
| `src/posture/tcn/tcn_model.py` | **New (Section 13).** TCN architecture (`build_tcn_model`) — causal dilated `Conv1D` residual blocks |
| `src/posture/tcn/tcn_trainer.py` | **New (Section 13).** Trains the TCN on the same `data/lstm_dataset.npz` as the LSTM; mirrors `lstm_trainer.py`'s split/augmentation/callbacks exactly |
| `src/posture/tcn/tcn_classifier.py` | **New (Section 13).** Production inference wrapper (`TCNPostureClassifier`), same public interface as `LSTMPostureClassifier` — drop-in replacement |
| `compare_tcn_lstm.py` | **New (Section 13).** Side-by-side LSTM-vs-TCN evaluator on labelled real footage; `--lstm-model`/`--tcn-model` let it point at any checkpoint |
| `hybrid_evaluate.py` | Batch evaluation: heuristic vs. LSTM vs. hybrid, scored against ground truth |
| `evaluate_real_footage.py` | Heuristic-only batch evaluator; also owns the ground-truth parser (`load_ground_truth`, `get_fall_window`) shared by `hybrid_evaluate.py` |
| `realtime_fall_detection.py` | **New.** Live camera/video → hybrid pipeline → debounced alert |
| `verify_labels.py` | Diagnostic — checks % of Fall training windows that contain a real transition frame (should always read 100%) |
| `docs/sanity_check_clips.md` | **New.** Recording guide (camera setup, clip list, GT format) for validation footage |
| `INSTRUCTIONS.md` | **New.** Setup + run instructions, including live-camera usage |
| `tests/test_lstm_pipeline.py` | Unit tests for the LSTM pipeline (21 tests, all passing as of this revision) |
| `models/lstm_posture.keras` / `lstm_label_encoder.json` | Current production model + encoder (includes `col_medians`); NEVER overwritten by the TCN work in Section 13 |
| `models/lstm_posture_before_lefd.keras` / `lstm_label_encoder_before_lefd.json` | Pre-LeFD, pre-feature-fix backup, kept for before/after comparison |
| `models/lstm_posture_retrained.keras` / `lstm_label_encoder_retrained.json` | **New (Section 13).** LSTM retrained on the exact same `data/lstm_dataset.npz` as the TCN, for a fair same-data comparison — a separate file from the production model above, never used in production |
| `models/tcn_posture.keras` / `tcn_label_encoder.json` | **New (Section 13).** Trained TCN model + encoder |
| `data/processed_keypoints/pose_keypoints.csv` / `posture_output.csv` | Frame-level keypoints + labels (not committed to git — see `.gitignore`; regenerate via `build_lstm_datasets.py`, or `build_ur_dataset_from_data_root.py` if raw data is under `data/ADL`/`data/Fall`) |
| `data/lstm_dataset.npz` | Window-level training data, shared by the LSTM and TCN (not committed; regenerate via `lstm_dataset.py`) |
| `data/ADL/`, `data/Fall/` | **New (Section 13).** Raw UR Fall Detection Dataset frame sequences (40 ADL + 30 Fall, 11,936 frames total), not committed to git |
| `test_footage/` | **New (Section 13).** 9 labelled real fall clips (recorded by the user) used to evaluate the LSTM/TCN comparison; not committed to git |
| `results/tcn_vs_lstm/`, `results/tcn_vs_lstm_fair/` | **New (Section 13).** Generated comparison reports — the former is production-LSTM-vs-TCN, the latter is the same-data, fixed-seed comparison (see Section 13) |
| `TCN_IMPLEMENTATION_NOTES.md` | **New (Section 13).** Standalone doc: how a TCN works, why it was chosen, LSTM/TCN differences, files added/modified, the reproducibility finding, how to train/compare |
| `Testing/` | All recorded validation footage, organized by contributor+date (not committed to git — large video files) |
| `context.txt` | This file |

---

## 12. Changelog Against the Previous Revision of This File

The previous revision of this document predated the LeFD integration and described the LSTM false-positive crisis as actively unresolved ("Pending: Retrain LSTM on the new clean dataset"), with three diagnostic tasks (window composition, sitting FP mechanism, geometric coordinate comparison) as the most recent findings. Everything from Section 4 onward in this revision happened after that point. The original diagnostic tasks and the Flaw A relabeling fix they led to are still historically accurate and are the reason the dataset window-labeling logic looks the way it does in `lstm_dataset.py` today (see Section 3's "Original diagnostic tasks" subsection), but are no longer restated in full here — the root cause they were chasing (Flaw B, "absolute feature representation") is the same one fully resolved in Section 3.

---

## 13. TCN Alternative Architecture & Reproducibility Investigation (August 2026)

### Why
The LSTM (Section 3) is a reasonable but not the only architecture for classifying the same 30-frame × 132-feature window. A Temporal Convolutional Network (TCN — causal, dilated `Conv1D` stack with residual connections, Bai et al. 2018) is the standard convolutional alternative to an RNN for sequence classification, and consumes the *exact same* `lstm_features.py` input, making it a clean apples-to-apples comparison point: same task, same window, same input representation, different temporal mechanism.

### Architecture (`src/posture/tcn/tcn_model.py::build_tcn_model`)
```
Input (30 frames × 132 features)
  → 4 residual blocks, dilations 1/2/4/8, each:
      Conv1D(32, kernel=3, causal, dilation=d) → LayerNorm → ReLU → Dropout
      Conv1D(32, kernel=3, causal, dilation=d) → LayerNorm → ReLU → Dropout
      + residual add (1x1 Conv1D projection when channel counts differ)
  → GlobalAveragePooling1D → Dense(5, softmax)
```
Receptive field: 31 frames (covers the whole window from the last block alone). 39,365 parameters vs. the LSTM's 63,013 — not forced to match, just reported honestly. `LayerNormalization` (not `BatchNormalization`) was chosen specifically because it normalizes per-sample, so behavior at single-window inference (batch size 1) matches training instead of depending on batch statistics.

### Data pipeline built for this
No windowed training dataset existed yet in this checkout — `data/` only contained raw UR Fall Detection Dataset frames (`data/ADL/`: 40 sequences, `data/Fall/`: 30 sequences, 11,936 frames total), dropped directly under `data/` rather than the `datasets/UR_data/` layout `build_lstm_datasets.py` expects. Rather than modifying that existing script or duplicating its MediaPipe-extraction logic, a new one-off adapter (`src/data_processing/build_ur_dataset_from_data_root.py`) imports and reuses its `process_ur_sequence` function unchanged, pointed at the new location. Output: `data/processed_keypoints/pose_keypoints.csv`/`posture_output.csv` (same paths `lstm_dataset.py` already reads) → `python src/posture/lstm/lstm_dataset.py` → `data/lstm_dataset.npz`, 9,906 windows across 70 sequences (Fall 24.2%, Lying 15.2%, Sitting 9.2%, Standing 40.0%, Unknown 11.3%).

### Result: same-data comparison (`results/tcn_vs_lstm_fair/`)
The LSTM was also retrained on this exact same `data/lstm_dataset.npz` (via the unmodified `lstm_trainer.py`, saved to `models/lstm_posture_retrained.keras` — a separate file, the production `models/lstm_posture.keras` was never touched) so the comparison reflects architecture, not training-data differences. Evaluated on all 8 valid clips in `test_footage/` (a 9th, `Forward_fall_gt.csv`, has no matching video — see below):

| Metric | LSTM (retrained, same data) | TCN |
|---|---|---|
| Accuracy | 74.1% | 76.0% |
| Fall-detection recall | 75.0% (6/8) | 87.5% (7/8) |
| Parameters | 63,013 | 39,365 |
| Latency mean | 57.9 ms/window | 55.1 ms/window |

The TCN comes out ahead on every axis in this run. `Backward_fall.mp4` was missed by the TCN too (same single-camera 2D-projection limitation as Section 2's heuristic weakness — confirms it's a data/geometry limitation, not specific to any one architecture).

**Filename bug found, not fixed**: `test_footage/Forward_fall_gt.csv` doesn't match `test_footage/Foward_fall.mp4` (missing "r"), so `discover_clips()`'s stem-matching skips it — same class of issue as `Testing/Sanawar Testing 7-25-26/`'s `Foward_fall.mp4` typo noted in Section 8c. Left unfixed (not renamed) per the same policy as that earlier instance — the user's recorded file is not renamed without being asked.

### The hyperparameter sweep and what it actually found
A sweep of ~4 hyperparameter variants per model (wider/narrower layers, dropout, learning rate) was run and initially looked like it found real improvements by validation accuracy ("TCN wider": 48 filters, "LSTM narrow_lowdrop": 48/24 units). **On real footage, both regressed** (fall recall 87.5% → 75%, both missing an additional clip they'd previously caught). Root-caused to: **model weight initialization was unseeded in both trainers** — retraining the exact same default hyperparameters twice produced different real-footage results (75% vs. 87.5%) from random init alone, before any hyperparameter was even changed. `keras.utils.set_random_seed(42)` was added to both `lstm_trainer.py::train()` and `tcn_trainer.py::train()` to reduce this, but full determinism would additionally need `tf.config.experimental.enable_op_determinism()` (not enabled — has its own performance cost, and TF's CPU convolution/LSTM ops can still be non-deterministic across process runs even with a fixed seed). **Conclusion: with only 8 real clips, one flipped clip is a 12.5-point swing in fall recall — large enough that this sweep's apparent hyperparameter effects cannot be distinguished from training noise.** The final models kept are the **original default hyperparameters** for both architectures, not the sweep's nominal winners. A trustworthy hyperparameter search would need multiple seeds averaged per config and/or a substantially larger real-footage evaluation set. Full detail: `TCN_IMPLEMENTATION_NOTES.md` Section 6.

### Two more (minor) bugs fixed as part of this work
- `lstm_trainer.py`/`tcn_trainer.py`: `val_split` was accepted as a parameter (and exposed via `--val-split`) but silently ignored — the split always used a hardcoded `StratifiedGroupKFold(n_splits=5, ...)` regardless of the value passed. Fixed to derive `n_splits` from `val_split`; default behavior (0.20 → 5 splits) unchanged.
- `compare_tcn_lstm.py`'s auto-generated report had a section (data provenance) that hardcoded "the LSTM was NOT retrained for this comparison" — true for the very first run only, and silently wrong once a retrained checkpoint was evaluated via `--lstm-model`. Now reads the actual checkpoint path from the classifier and describes it dynamically.

### Files added/modified — see `TCN_IMPLEMENTATION_NOTES.md` for the complete list
Summary: `src/posture/tcn/{__init__.py, tcn_model.py, tcn_trainer.py, tcn_classifier.py}` and `src/data_processing/build_ur_dataset_from_data_root.py` are new; `compare_tcn_lstm.py` (new, repo root) evaluates both models under identical conditions and writes per-window CSVs + a markdown report with a discussion section generated from the actual measured numbers. `src/posture/lstm/lstm_trainer.py` and `src/posture/tcn/{tcn_model.py, tcn_trainer.py}` were modified (hyperparameter knobs, the seed fix, the `val_split` fix) — all additive/backward-compatible, default behavior unchanged. No file in Sections 1-12 above was touched otherwise (`lstm_features.py`, `lstm_dataset.py`, `lstm_classifier.py`, `pipeline_utils.py`, `realtime_fall_detection.py`, `evaluate_real_footage.py`, `hybrid_evaluate.py` are all untouched).

---

## 14. Changelog Against the Previous Revision of This File

The previous revision ended at Section 12 (LeFD/LSTM-era changelog) with the heuristic+LSTM hybrid pipeline validated and pending items limited to the camera-axis miss, elderly-fall data gap, and unconfirmed precision on new ADL footage (Section 10). This revision adds Section 13 in full: a second model architecture (TCN) implemented from scratch, a full raw-data-to-trained-model pipeline built for data that arrived in a new location (`data/ADL`/`data/Fall` instead of `datasets/UR_data/`), a same-training-data comparison against a freshly-retrained LSTM, and — the most consequential finding — direct evidence that this project's training pipeline was not reproducible run-to-run, which had been silently confounding any attempt to compare hyperparameters (or, by extension, architectures) with only a single training run each. That reproducibility gap is partially closed (Section 13) but not fully solved, and is now the top methodological item under Section 10's Pending list alongside the pre-existing elderly-fall-data and new-footage-precision gaps.

**Correction to line 215's historical "nothing pushed yet" note**: that work was pushed later the same branch (`TCN_sanawar`, commit `301b72d`) — see Section 15 below. **`INSTRUCTIONS.md`** (referenced throughout Sections 10-11 above) **no longer exists as a standalone file** — its content was merged into `README.md` during the documentation cleanup in Section 15; treat `README.md` as the current "how do I run this" entry point.

---

## 15. Session Handoff — TCN Regression Fix, Random Forest (+ Pruning), Gait Module, Doc Cleanup (August 2026)

This section is a self-contained handoff for a fresh AI session — read this first, then dip into Sections 1-14 above for historical *why*, and `README.md` for practical "how do I run this."

### 15.1 Project (recap)

**Purpose**: detect falls from camera footage (heuristic + LSTM/TCN/Random Forest, OR-gated) and, separately/experimentally, assess longer-term fall *risk* from gait quality (`src/gait/`). Target deployment: Jetson Nano/Orin edge hardware — model size/latency/RAM are tracked alongside accuracy throughout.

**Current objective going into this session**: fix a measured TCN accuracy regression, add Random Forest as a third model, reduce RF overfitting via pruning, and implement `assess_risk(window)` for the gait/fall-risk workstream (`docs/IMPLEMENTATION_PLAN.md` Section 3, "GAIT_hussain" — pulled forward onto this branch by explicit user instruction, not the norm; see 15.7).

### 15.2 Architecture (recap — see README.md Section 17 for the full tree)

Three interchangeable temporal classifiers share one contract
(`.predict(window) -> {"posture_label", "fall_detected", "confidence", "other_labels"}`,
`.window_size`, `.raw_history_needed`, `.is_available`):
`LSTMPostureClassifier`/`TCNPostureClassifier` (`src/posture/sequence_window_classifier.py`
base class) and `RFPostureClassifier` (`src/posture/rf/`, reimplements the
same interface since it flattens the window instead of consuming it as a
sequence). All three train on the identical `data/lstm_dataset.npz`.

`src/gait/` (`gait_features.py` + `gait_risk.py::GaitRiskAssessor`) is a
**conceptually separate** module — longer windows (>=90 frames), a risk
SCORE output (not fall/no-fall), no shared code with the classifiers above
beyond read-only imports from `pipeline_utils.py`/`lstm_features.py`.

**Files nobody should edit without raising it with the team first**
(`docs/IMPLEMENTATION_PLAN.md` Section 0): `src/posture/pipeline_utils.py`,
`src/posture/lstm/*`, `hybrid_evaluate.py`, `evaluate_real_footage.py`,
`realtime_fall_detection.py`. None of this session's work touched them
(verified via `git status` before finalizing — see 15.8).

### 15.3 The TCN regression: what happened and the fix

Entering this session, working-tree (uncommitted) changes to
`src/posture/tcn/` had regressed real-footage accuracy from 76.0% to 47.8%
(`test_footage/Sanawar Testing 7-22-26`, 8 clips) and fall recall from
87.5% to 75%. A 4-config ablation (baseline / L2-only / class-weights-only
/ both) isolated **`use_class_weights=True`** (added alongside L2
regularization in the same prior session, both defaulted on without a
same-footage before/after check) as the sole cause: "balanced" class
weighting fixed the `Unknown` class's near-zero recall by making the model
predict `Unknown` far more liberally overall, which collapsed accuracy on
real footage (~90% `Lying` frames) since `Unknown` predictions nearly
tripled at the expense of correct `Lying`/`Standing` calls. L2=1e-5 alone
was confirmed harmless (even mildly positive: 76.0% -> 76.9%).

**Fix**: `use_class_weights` now defaults to `False` in `tcn_trainer.py`
(kept as a parameter, not removed, for future work with a proper
before/after check). The same parameter was added to `lstm_trainer.py`
(also defaulting `False`) purely for API parity across all three trainers.
Corrected TCN retrained and promoted to `models/tcn_posture.keras` —
76.9%/87.5% on the apples-to-apples pre-merge check, matching/exceeding
the previous best. Full ablation tables: `docs/TCN_IMPLEMENTATION_NOTES.md`
Section 6.5. Full writeup incl. the 3-way final comparison:
`docs/TCN_REGRESSION_REPORT.md`.

**Known follow-on wrinkle, not fixed in this session**: merging `origin/main`
in (see 15.4) changed real-footage keypoint extraction (MediaPipe IMAGE ->
VIDEO tracking mode), and under that new extraction the corrected TCN's
fall recall on the Sanawar set reads 75% instead of 87.5% (misses
`Backward_fall` in addition to `Far_fall`). Confirmed unrelated to the
class-weighting fix (the pre-merge apples-to-apples check above already
shows the fix alone preserves 87.5%) — this is an unexplained interaction
between the TCN specifically and the extraction-pipeline change, flagged
as follow-up work, not chased further this session.

### 15.4 Branch merge

Per explicit user request, `origin/main` was merged into `TCN_sanawar`
before finalizing (commit `775c419`). Clean, no conflicts — confirmed via
diff that main's changes (`b566685`: MediaPipe visibility-masking,
VIDEO-mode tracking, hybrid pipeline fixes) touch zero files this branch's
TCN/LSTM/RF/gait work depends on. All 37 pre-existing unit tests passed
post-merge before any further session work began.

### 15.5 Random Forest + pruning

`src/posture/rf/rf_trainer.py` + `rf_classifier.py`: trains on the exact
same dataset/split/augmentation as LSTM/TCN, flattening each `(30, 132)`
window to `3960` features (a tree ensemble has no time axis).
`RFPostureClassifier` reuses `extract_raw_keypoints`/`run_classifier_cli`
from `sequence_window_classifier.py` but does not subclass it (joblib
model, not a Keras one).

**A real inference-time bug was found and fixed**: `RandomForestClassifier`
was loaded with its training-time `n_jobs=-1` still set, and
`predict_proba()` re-parallelizes across trees on *every single call* —
fine at training time (parallelizing over the whole dataset once), but
catastrophic when called once per video frame (spinning up a joblib worker
pool per single-row prediction is pure overhead with nothing to
parallelize). Observed as a real-footage evaluation run ballooning to
3.75 million lines of repeated warnings before being killed. **Fix**:
`RFPostureClassifier._load()` now forces `self._model.n_jobs = 1`
immediately after loading; training scripts are unaffected (they set their
own `n_jobs` independently). If a future session sees an RF evaluation
"hang" or produce a runaway log, check this first.

**Pruning, round 1**: a 10-config sweep (`max_depth` in {10,15,20},
`min_samples_leaf` in {10,20}, `ccp_alpha` in {5e-4,1e-3}, plus a combined
depth+leaf+fewer-trees config) confirmed on both real-footage sets (not
just the validation split). `min_samples_leaf=20` won outright: train/val
gap 0.289 -> 0.250, macro F1 0.550 -> 0.592 (validation) / 0.498 -> 0.511
(Hussain real footage), model size 27.7MB -> 7.8MB (72% smaller). Set as
`rf_trainer.py::MIN_SAMPLES_LEAF`.

**Pruning, round 2** (a later sub-session, asked to reduce the gap
further): an 11-config sweep tried `max_features`, `max_samples`
(bootstrap subsample fraction), higher `min_samples_leaf`, and
combinations on top of `min_samples_leaf=20`. `max_features=0.1` (each
split considers ~396 of the 3960 flattened features, vs.
`RandomForestClassifier`'s own `sqrt(3960)`~=63 default) won on real
footage: validation gap 0.250 -> 0.238, and — the more decision-relevant
number — Hussain real-footage accuracy 60.5% -> 69.2% (macro F1 0.511 ->
0.565) at a cost of only ~1 point on the easier Sanawar set (56.2% ->
55.2%), fall recall unchanged (100%/87.5%) on both. A combined
`max_features=0.1` + `max_samples=0.5` config closed the validation gap
slightly further (0.218) but did *worse* on real footage than
`max_features=0.1` alone (more false alarms, lower Hussain accuracy) — not
used, another instance of validation-split numbers not telling the whole
story. Set as `rf_trainer.py::MAX_FEATURES`. **Current production
`models/rf_posture.joblib`**: 92.74% train / 68.92% val accuracy (gap
23.8%, down from the original unpruned 28.9%).

**Final 3-way real-footage result** (`compare_all_models.py`,
`results/all_models_{sanawar,hussain}/`, regenerated against the
round-2-pruned RF checkpoint): TCN best on the easier Lying-dominated
Sanawar set (79.1% accuracy; RF 55.2% there, down slightly from
pre-pruning's 57.3%); Random Forest best on the harder Hussain edge-case
set by an even wider margin post-pruning (69.2% accuracy, up from 60.3%
pre-pruning; macro F1 0.565; still the only model catching every labelled
fall, now with one fewer false alarm too — 5 vs. 6). RF's node count and
model size also dropped sharply from pruning (271,508 -> 69,212 nodes;
27.7MB -> 7.2MB) and its measured inference latency roughly halved (less
work per split with `max_features=0.1`). No single winner regardless; see
`docs/TCN_REGRESSION_REPORT.md` for the full deployment-tradeoff
discussion.

### 15.6 Gait / fall-risk module (`src/gait/`)

Implements exactly the interface `docs/IMPLEMENTATION_PLAN.md` Section 3
specifies: `GaitRiskAssessor.assess_risk(window) -> {"risk_score": float |
None, "signals": [...]}`. **Not a fall/no-fall classifier** — see
README.md Section 15 for the full contract. Four rule-based signals
(`gait_features.py`): `compute_walking_speed`, `compute_stride_regularity`,
`compute_postural_sway`, `compute_sit_to_stand` — literature grounding in
`docs/GAIT_LITERATURE_REVIEW.md`, honest (there is no real gait dataset
anywhere in this project) validation writeup in
`docs/GAIT_DATA_ASSESSMENT.md`.

**Three real implementation bugs were found and fixed via validation, not
just "the code runs"** — worth knowing about before touching this module
again:
1. Position-based signals initially reused `lstm_features.normalize_frame`'s
   hip-CENTERED coordinates (each frame re-centered on its own hip origin)
   — correct for posture classification, but makes hip position (0,0) in
   every frame by construction, silently destroying the translation signal
   walking-speed/sway need. Fixed with a new `_torso_scaled_hip_track()`
   (scales by torso length like `normalize_frame` does, but does NOT
   re-center each frame).
2. Finite-differencing raw position into velocity amplifies per-frame
   noise badly at 30fps — added a NaN-aware centered moving-average inside
   `_torso_scaled_hip_track`. Real production robustness fix (real
   MediaPipe jitter would cause the same problem), not just a test patch.
3. Two risk-mapping thresholds (`gait_risk.py::_stride_cv_risk`,
   `_speed_risk`) originally reused literature-reported absolute numbers
   (IMU stride-time CV ~5%; an isolated debug measurement for speed) that
   didn't match this proxy's own actual achievable scale, saturating both
   sigmoids near a constant value regardless of input and destroying their
   ability to discriminate anything. Both recentered on this module's own
   observed synthetic-validation scale instead — explicitly documented as
   scale-matching choices, not clinical thresholds (see code comments).

`tests/test_gait_risk.py` (18 tests, all passing): output-contract checks,
input validation (`None`/empty/too-short/wrong-type/missing-keys),
NaN/Inf/occlusion robustness, an integration smoke test against real
(unlabeled) `data/processed_keypoints/pose_keypoints.csv` sequences, and
regression tests for the three bugs above (steady-vs-unsteady walking,
smooth-vs-shaky sit-to-stand must keep scoring in the correct direction).

**This module is NOT clinically validated** — no dataset with real
fall-risk outcomes exists anywhere in this project (confirmed in
`docs/GAIT_DATA_ASSESSMENT.md`); every risk threshold is heuristic and
marked `"calibrated": False` in its own output. Do not present its output
as validated without new real data.

### 15.7 A scope note future sessions should know about

`docs/IMPLEMENTATION_PLAN.md` (written to set up three parallel branches —
`TCN_sanawar`/`YOLO_fatima`/`GAIT_hussain` — so team members' work doesn't
collide) explicitly scopes gait/fall-risk work to the separate
`GAIT_hussain` branch, as its own module with its own data-collection
effort. Mid-session, this was flagged explicitly to the user (a real
architecture fork, not a small decision), and the user explicitly chose to
have the gait module built on `TCN_sanawar` anyway. `src/gait/` therefore
now exists on this branch rather than `GAIT_hussain` — a deliberate,
user-confirmed deviation from the original plan, not an oversight. Worth
surfacing to the rest of the team before/when merging, since it may affect
what's left for whoever works on `GAIT_hussain`.

### 15.8 Documentation cleanup

Per explicit user instruction: consolidated to exactly one root-level
`README.md` (previously a 2-line stub, now the full practical entry point
covering architecture/setup/training/evaluation/inference/testing/
limitations/status/future-work). Removed (content merged into `README.md`
and/or already superseded by this file or `docs/TCN_REGRESSION_REPORT.md`):
`INSTRUCTIONS.md`, `task.md` (stale — still described Phase 4/4.5 as
current, predating even the first LSTM training run), `summary.md`
(self-described as a redundant recap of this file's Section 13 +
`docs/TCN_IMPLEMENTATION_NOTES.md`), `Comparison.md` (superseded by
`docs/TCN_REGRESSION_REPORT.md`'s corrected, complete 3-way numbers),
`fixes.md` (a pre-implementation fix *proposal* whose adopted ideas are
already documented, as implemented, in this file's Sections 2/5). Moved
(not deleted — genuinely load-bearing technical reference, just not a
*second* root README): `TCN_IMPLEMENTATION_NOTES.md` and
`TCN_REGRESSION_REPORT.md` -> `docs/`. `docs/` itself was left otherwise
intact (`IMPLEMENTATION_PLAN.md`, `LSTM_Phase_Summary.md`,
`mediapipe_pose_reference.md`, `sanity_check_clips.md`, `recording_round2.md`,
`setup.md` are all still-relevant reference/protocol docs, not stale
narrative) plus two new files: `GAIT_LITERATURE_REVIEW.md`,
`GAIT_DATA_ASSESSMENT.md`.

### 15.9 Current state

**Done**: TCN regression fixed and validated; RF implemented, pruned, and
validated; 3-way real-footage comparison complete; `assess_risk()`
implemented, unit-tested (18/18 passing), and validated as far as
synthetic data allows; all 37 pre-existing tests + 18 new gait tests pass;
documentation consolidated. **Not done / known gaps**: no real gait-risk
validation data exists anywhere (top priority if `src/gait/` work
continues); the post-merge TCN fall-recall regression (15.3) is
unexplained; no YOLO/object-detection layer exists yet
(`docs/IMPLEMENTATION_PLAN.md` Section 5's integration phase hasn't
started); RF's train/val gap (92.7%/68.9%) is reduced by two rounds of
real-footage-confirmed pruning but not eliminated -- a follow-up
generalization investigation (§15.12) found this is a dimensionality
(3960 features / 56 independent sequences) issue, not leakage, and that
two further well-motivated remedies (a summary-statistic feature
representation, class weighting) both looked better on the validation
split but were correctly rejected after real-footage confirmation
contradicted that signal -- see `docs/RF_GENERALIZATION_INVESTIGATION.md`.
**Git state**: this session's work IS committed and pushed to
`origin/TCN_sanawar` (see 15.10 for exactly which commit) — a later
sub-session's RF-pruning-fix + gait-module work was done under an explicit
"do not commit, do not push" instruction and remains local/uncommitted as
of this writing; check `git status`/`git log` before assuming either way.

### 15.10a. Sub-session: RF generalization investigation (deeper diagnosis)

A follow-up sub-session was explicitly asked not to stop at "gap
improved, tests pass" and to genuinely diagnose whether the RF's
remaining ~24-point train/val gap was leakage or fixable. Full writeup:
`docs/RF_GENERALIZATION_INVESTIGATION.md`. Summary:

- **Leakage audit: clean.** Group split has zero sequence overlap.
  One real (but minor, not fixed) preprocessing-leakage source found:
  `col_medians` are computed over the full real dataset (train+val
  combined) in `lstm_dataset.py::build_dataset()`, before any trainer's
  split -- not fixed because `lstm_dataset.py` is shared, protected
  infrastructure (`docs/IMPLEMENTATION_PLAN.md` Section 0) and fixing it
  would require rebuilding the dataset and retraining all three models.
- **OOB score is untrustworthy for this data**: 0.889 vs. actual
  validation accuracy 0.689 -- RF's bootstrap resampling doesn't respect
  the sequence-group structure, so OOB silently overstates generalization
  on grouped/correlated data like this. Don't use it as a stopping
  criterion here.
- **Root cause: dimensionality, not leakage.** 3,960 flattened features
  from only 56 independent real training sequences; feature importance is
  diffuse (no single leaky feature to blame).
- **Two remedies tried, both correctly rejected after real-footage
  confirmation**: a summary-statistic feature representation (660 dims
  instead of 3960) won on every metric in a 5-fold group-aware CV, but
  lost clearly on the harder Hussain set (57.1% vs. 69.2% accuracy) when
  actually checked against real footage -- the third time in this
  project's history that a validation-split conclusion didn't survive
  real-footage contact. Class weighting (`use_class_weights=True`)
  reproduced the exact same failure signature already found and reverted
  for the TCN: it dropped fall-detection recall on BOTH real test sets
  (87.5%->75% Sanawar, 100%->75% Hussain) for marginal accuracy/F1 gains
  elsewhere -- disqualifying given this project's "a missed fall is worse
  than a false alarm" priority. Both are kept in the code as documented,
  available-but-not-default options (`rf_trainer.py`'s
  `feature_representation`/`use_class_weights` params), not deleted.
- **Conclusion**: the existing two-round-pruned config
  (`min_samples_leaf=20`, `max_features=0.1`, flatten representation,
  no class weights) remains the best real-footage-validated
  configuration. `models/rf_posture.joblib` was retrained (byte-identical
  92.74%/68.92% numbers, confirming determinism) only to pick up new
  encoder metadata (`feature_representation`, `n_features_out`) added so
  future checkpoints self-describe which window->vector transform they
  need at inference time. `tests/test_rf_classifier.py` (7 new tests)
  covers this switch plus the n_jobs=1 inference fix and prediction
  determinism -- full suite is now 62 tests (55 + 7), all passing.

### 15.10 Dependencies

`requirements.txt` updated: added `joblib` (RF serialization, was already
a transitive scikit-learn dependency but is now imported directly) and
`scipy` (`gait_features.compute_stride_regularity`'s peak detection). No
packages removed — the existing flat pip-freeze-style list mixes direct
dependencies with mediapipe/matplotlib's transitive sub-dependencies, and
a deeper prune wasn't attempted (risk of breaking a transitive need
outweighs the benefit of a marginally shorter file).

### 15.11 Important instructions for future AI sessions

- Read this file (Section 15 first, then whichever earlier section is
  relevant), then `README.md`, before touching anything.
- Run `git status` and `git log --oneline -10` before modifying anything —
  do not assume the state described here is still current; this document
  decays the moment new work lands.
- Do not commit or push unless explicitly instructed — multiple sessions
  in this project's history have been asked to keep work local-only.
- Do not edit `pipeline_utils.py`, `src/posture/lstm/*`, `hybrid_evaluate.py`,
  `evaluate_real_footage.py`, or `realtime_fall_detection.py` without
  raising it with the user first — see 15.2 and
  `docs/IMPLEMENTATION_PLAN.md` Section 0.
- Do not assume a feature is implemented without reading the actual code —
  this file, like any snapshot, can drift from reality.
- Do not introduce an external API (REST/Flask/FastAPI/cloud) for
  `assess_risk()` or any model inference path — everything in this project
  is a local, directly-callable Python interface by design.
- Preserve the `assess_risk(window) -> {"risk_score", "signals"}` contract
  exactly — it is deliberately NOT a fall/no-fall flag; see README.md
  Section 15 and `docs/IMPLEMENTATION_PLAN.md` Section 3 for why that
  distinction is load-bearing, not stylistic.
- Run the test suite after any change:
  `python -m unittest tests.test_lstm_pipeline tests.test_posture_pipeline tests.test_gait_risk tests.test_rf_classifier -v`
  (66 tests as of Section 16 — the 3-module command in earlier revisions of
  this file predates `tests.test_rf_classifier` and silently skipped it).
- Before trusting any tuning decision (hyperparameter, threshold, pruning
  config), check it against real footage
  (`test_footage/Sanawar Testing 7-22-26`, `test_footage/Hussain Testing 7-30-26`),
  not just the validation split — this project has been burned by
  validation-split-only conclusions twice now (Section 13's hyperparameter
  sweep, and 15.3's class-weighting regression). Both are cautionary
  tales, not one-off incidents.

---

## 16. Project Cleanup & Validation Audit (August 2026)

A full repo audit: dead-file cleanup, `context.txt`/`README.md` accuracy
pass, actual execution (not just code inspection) of the gait/fall-risk
module and the model-comparison pipeline, a `models/` review, a light
code-quality pass, and a full test-suite run. Read this section first if
you're picking the project up after this session; Sections 1-15 above are
still the accurate history of *why* the system looks the way it does.

### 16.1 Files removed, and why

**`results/`** — 17 subdirectories and ~19 loose files were deleted. All of
it was superseded parameter-sweep/testing-round output (`heuristic_retuned4`,
`hybrid_after_features`, `hybrid_after_gate`, `hybrid_after_lefd`,
`hybrid_consec6`, `hybrid_extended_window`, `hybrid_final`,
`hybrid_warmup45`, `new_test_heuristic_extended`, `new_test_hybrid_extended`,
`hussain_testing_heuristic`, `hussain_testing_hybrid`, `hybrid`, `testVideo`,
`test_footage_extended_window`, `Hussain_Testing_7-30-26`,
`Sanawar_Testing_7-22-26`, `Testing_Sanawar_7-22-26`, `forward_fall_single`),
plus loose stale prediction CSVs, a stray diagnostic log from an unrelated
branch (`pipeline_fixes_log.md`, dated 16 Jul 2026, branch `fatima`), an
outdated `real_footage_results.md`, and a scratch file (`temp_md.md`, which
had the same section triplicated). Verified via `git log` per-path and a
repo-wide grep that none of these were referenced by any script, test, or
doc — every number/finding they contained is already captured narratively
in Sections 2-15 above. **Kept**: `results/all_models_sanawar/`,
`results/all_models_hussain/` (current 3-way comparison, cited by
README.md/context.txt), `results/tcn_vs_lstm/`, `results/tcn_vs_lstm_fair/`
(Section 13's same-data comparison, cited by `docs/TCN_IMPLEMENTATION_NOTES.md`).

**`models/experiments/`** and **`models/lstm_checkpoint/`** — removed.
`models/experiments/lstm_v4_round2_gt.keras` was confirmed **byte-identical**
(MD5) to the current production `models/lstm_posture.keras` — a pure
duplicate. `models/experiments/lstm_v3_vetoes_on.keras` and
`models/lstm_checkpoint/lstm_posture.keras` were both earlier rollback
candidates from a model-selection decision that `models/lstm_checkpoint/README.md`
itself documented as "SUPERSEDED 2 Aug 2026" (see 16.2 below for what that
README revealed — a decision-history gap in this file, now closed). Neither
directory was referenced anywhere outside its own now-deleted README.

Nothing under `data/`, `datasets/`, `Testing/`, or `test_footage/` was
touched — all still gitignored/untracked exactly as before.

### 16.2 A documentation gap this audit found and closed (not a code change)

`models/lstm_checkpoint/README.md` (now deleted, its history preserved
here) revealed that `src/posture/pipeline_utils.py` has an
`ENABLE_UPRIGHT_VETOES = True` feature — two "positive evidence" vetoes
(`legs_say_upright`, a leg-angle + body-extent check; `hips_still_high`, a
hip-height-vs-standing-baseline check) that suppress a false "Lying"
classification while someone is bending/squatting rather than actually
lying down. This was merged in from `main` (commit `b566685`, "Fix live
false alarms...") and is genuinely active in the current pipeline, but
**Sections 1-15 of this file never mention it** — a real gap, since it's
exactly the kind of load-bearing threshold decision this file exists to
record. Per `docs/IMPLEMENTATION_PLAN.md` Section 0, `pipeline_utils.py`
itself was not touched by this audit (raise changes there with the team
first) — this is a documentation-only fix. Measured effect (from the now-
deleted README): with the current model, the vetoes took the Round-2
held-out negative-clip score from 10/18 to 17/18 clean with zero cost to
Round-3 fall recall (still 6/6) — they only matter with the current model
checkpoint, and did nothing on the prior one, which is why they'd sat
disabled for a while before being turned on.

### 16.3 Gait/fall-risk module — actually executed, not just read

`GaitRiskAssessor.assess_risk()` was run against all 70 real sequences in
`data/processed_keypoints/pose_keypoints.csv` (not just the unit tests,
though those were also re-run and still pass — 19/19). Of 70 sequences, 55
met the `MIN_WINDOW_FRAMES=90` floor; all 55 were scored with **zero
crashes and zero unhandled exceptions**:
- `risk_score` was never `None` for any of the 55 (mean 0.371, std 0.116,
  range 0.088-0.597) — every sequence had at least one usable signal.
- Signal availability: `walking_speed` 100% (55/55), `stride_regularity`
  89.1% (49/55), `postural_sway` 74.5% (41/55), `sit_to_stand` 60.0%
  (33/55) — consistent with `sit_to_stand`'s own docstring describing
  "no transition in this window" as the expected common case.
- Latency: median 28ms/window, mean 54ms (one outlier sequence at
  ~1.25s — the longest sequence in the set, 400 frames, still well within
  interactive/offline-analysis budgets; nowhere close to the file's own
  frame-rate budget since gait assessment is a periodic longer-window
  operation, not a per-frame one like the fall classifiers).
- No sequence produced a non-finite (`NaN`/`Inf`) `risk_score`, and no
  sequence had all four signals simultaneously unavailable.

This is real UR Fall Dataset ADL/Fall footage, not synthetic data, so it's
a genuine (if unlabeled — no ground-truth fall-risk outcome exists for
this data, see `docs/GAIT_DATA_ASSESSMENT.md`) smoke/plausibility result:
the module runs cleanly end-to-end on the full real corpus, not just the
synthetic scenarios in `tests/test_gait_risk.py`. It does **not** newly
validate the risk scores themselves against any ground truth — that gap
(Section 15.6, `docs/GAIT_DATA_ASSESSMENT.md`) is unchanged.

### 16.4 Code-quality fix applied

`src/posture/rf/rf_trainer.py` had two stale comments left over from when
`feature_representation="summary"` was briefly the intended default (before
`docs/RF_GENERALIZATION_INVESTIGATION.md`'s real-footage check rejected it
— see Section 15.10a/15.9): the `--feature-representation` CLI help text
literally said `"(default: summary)"`, and a `NOTE` comment above
`MAX_FEATURES` asserted `FEATURE_REPRESENTATION` "defaults to summary."
Both were wrong — `FEATURE_REPRESENTATION = "flatten"` is the actual
in-code default and matches the production checkpoint
(`models/rf_label_encoder.json`'s `"feature_representation": "flatten"`,
confirmed by reading it directly). Fixed both comments to describe the
code as it actually behaves; no behavior change.

### 16.5 Test suite — full run

`python -m unittest tests.test_lstm_pipeline tests.test_posture_pipeline
tests.test_gait_risk tests.test_rf_classifier` (the full suite, all four
modules, 21+16+19+10 = **66 tests**): **66/66 passed**, both before and
after the cleanup in 16.1 (run twice to confirm the deletions caused zero
regressions). No skips observed in this environment (all three model
checkpoints and `data/processed_keypoints/` were present). One pre-existing
`DeprecationWarning` (`datetime.datetime.utcnow()` in `pipeline_utils.py`)
— cosmetic, not a failure, not fixed here since it's inside the
do-not-edit-lightly file.

### 16.6 What this audit did NOT change

No training data, no model checkpoints (beyond deleting the two duplicate/
superseded ones in 16.1), no `pipeline_utils.py`, no `lstm_features.py`, no
`hybrid_evaluate.py`, no `evaluate_real_footage.py`, no
`realtime_fall_detection.py`, no `src/gait/` logic — all per the same
do-not-edit-without-raising-it-first list Section 15.2 already established.
The `models/` directory's internal layout (flat, one file per checkpoint)
was reviewed and deliberately left as-is — see README.md's Project
Structure section for the reasoning (splitting into per-architecture
subfolders would require updating every trainer's and classifier's default
path constants for no real discoverability gain at this project's current
size — 11 files, already namespaced by filename prefix).
